{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Jekyll",
      "type": "shell",
      "command": "set JEKYLL_ENV=production; bundle exec jekyll serve --incremental",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}